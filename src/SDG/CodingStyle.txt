C++ Standard 17
Use pImpl idiom to keep the SDL_gpu and SDL headers out of the SDG header files.
- Make sure to initialize variables in the Impl struct in the constructor.

An object containing a raw pointer signifies that that object owns
it, and must handle memory management for it.
